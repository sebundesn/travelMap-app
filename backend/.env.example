# Copy to .env for local dev (loaded by docker-compose / your shell).
DATABASE_URL=postgres://travelmap:travelmap@localhost:5432/travelmap?sslmode=disable
PORT=8080
FRONTEND_ORIGIN=http://localhost:3000
JWT_SECRET=dev-secret-change-me
UPLOAD_DIR=./uploads
