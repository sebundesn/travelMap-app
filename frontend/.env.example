# Copy to .env.local for local dev. In Docker, these are passed as --build-arg
# instead (see Dockerfile) since NEXT_PUBLIC_* values are inlined at build time.
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=
